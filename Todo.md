## 能力扩展
1. SecurityChecker 增加命令检查 检查执行的命令是否在白名单，并且参考现在的path，支持配置文件定义，也支持动态增加 
2. 为工具包的文件操作实现预查接口支持判断路径是否允许操作
3. 实现一个支持执行命令行的工具，并为其实现预查方法
4. 检查当然无用的Hook阶段 去掉


## 项目级配置融合


## 日志系统
1. 检查所有的Print位置全部改为日志
2. 日志需要区分不同文件，不耦合，在配置包增加日志配置项包括等级和位置，默认为caelix——home下的logs
3. 日志输出为每行都是Json
4. 目前包含


## 待构思
日志的写入和console 分离，项目级配置融合，记忆，压缩，记忆卸载
上下文窗口记录 用量统计，ast 接入，
技能包概念，技能和工具的列表以及技能内脚本
在根项目增加最新的tree-sitter依赖 在工具包增加 基于tree-sitter 是实现一个ast工具支持查看指定文件的内的所有方法名何其开始到结束的行数 